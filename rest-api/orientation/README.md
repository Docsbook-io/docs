---
title: "Orientation"
description: "Find out what this server is, which project the user means, and — on an owner's token — what else this connection can already call."
---

# Orientation

Find out what this server is, which project the user means, and — on an owner's token — what else this connection can already call.

<!-- widget:endpoints -->

- [Get info](./get-info.md) `GET /api/v1/get_info` — What this Docsbook MCP server is and how to work it: the plan tiers and what each unlocks, every tool family with the…
- [Get workspace](./get-workspace.md) `GET /api/v1/get_workspace` — Get one project in full — every setting an update_* tool can change, its plan and capabilities, its call to action,…
- [List workspaces](./list-workspaces.md) `GET /api/v1/list_workspaces` — List your Docsbook documentation projects — one line each: id, repo, name, live URL, plan, whether the assistant is…

<!-- /widget -->
